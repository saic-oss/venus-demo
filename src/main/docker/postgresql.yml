version: '2'
services:
  Venus-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Venus/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Venus
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
